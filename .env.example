OPENAI_API_KEY=
ANTHROPIC_API_KEY=
HUGGINGFACEHUB_API_TOKEN=

# Optional: set a secret to enable HMAC-SHA256 integrity verification for FAISS indexes.
# When set, each index is signed after processing and the signature is verified before loading.
# A tampered or substituted index file will be rejected.  Leave blank to disable (default).
FAISS_HMAC_SECRET=
